{
  "name": "container-monitoring-system",
  "version": "1.0.0",
  "description": "Container Monitoring System is a tool for monitoring Docker containers using a Bash script and an Express.js backend. The CLI script collects data on running containers, including their status and resource usage, and stores it in a database (MongoDB). Automation with a cron job ensures regular data updates.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongodb": "^6.15.0",
    "mongoose": "^8.13.1"
  }
}
